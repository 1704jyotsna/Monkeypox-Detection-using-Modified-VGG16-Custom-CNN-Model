# Monkeypox-Detection-using-Modified-VGG16-Custom-CNN-Model
This project uses deep learning to detect Monkeypox from skin images. A modified VGG16 model extracts features, and a custom CNN performs classification.  It processes images and predicts whether they are infected or normal. Built with Python and TensorFlow, the system is simple, accurate, and useful for early diagnosis.
